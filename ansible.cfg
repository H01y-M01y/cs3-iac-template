[default]
inventory = inventory/hosts.yml

roles_path = roles

host_key_checkin = False
retry_file_enabled = False
stdout_callback = default
result_format = yaml

[privilege_escalation]
become = True
become_method = sudo
become_user = root
